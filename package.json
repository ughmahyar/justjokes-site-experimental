{
  "name": "justjokes-site-copy",
  "private": true,
  "version": "0.1.0",
  "description": "just jokes: one-page site — local dev preview",
  "scripts": {
    "dev": "browser-sync start --server . --port 8080 --host 0.0.0.0 --no-open --watch --files index.html,assets/**"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}